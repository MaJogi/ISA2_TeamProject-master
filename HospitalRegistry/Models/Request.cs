using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace HospitalRegistry.Models
{
    public class Request
    {
        //Patient sends a form (request) and that data should be saved in sql database. After that registry should see requests
        //And make patients, if the patient isn't registred yet.
    }
}
